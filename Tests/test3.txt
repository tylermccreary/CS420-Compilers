program looper;
	# This is a real trupl program.
	# Here's the declarative part.
	a: int;
	b: int;
	c: int;
    d: bool;
    e: bool;
    f: bool;

    begin
        a := a;
        d := a < b;
        e := a <= b;
        f := a > b;
        d := a >= b;
        e := a = b;
        f := a <> b;
    end;
